/*
 *
 * LocaleToggle messages
 *
 */
import { defineMessages } from 'react-intl';

export const scope = 'efr-frontend.containers.LocaleToggle';

export default defineMessages({
  en: {
    id: `${scope}.en`,
    defaultMessage: 'English',
  },
  fr: {
    id: `${scope}.fr`,
    defaultMessage: 'French',
  },
});
