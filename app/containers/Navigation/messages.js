/*
 *
 * Navigation messages
 *
 */

import { defineMessages } from 'react-intl';

export const scope = 'efr-frontend.containers.Navigation';

export default defineMessages({
  appbarHeader: {
    id: `${scope}.appbar.header`,
    defaultMessage: 'Enterprise Flow Repository',
  },
});
